/**
 * Placeholder for the comment tree. B2 will land the real CommentTree component
 * in M6. This component holds the correct layout position in the meantime.
 */
export const CommentsPlaceholder = () => (
    <div
        id="comments-placeholder"
        className="rounded-xl border border-border/30 bg-secondary/20 px-4 py-6 text-sm text-muted-foreground"
        aria-label="Comments loading"
    >
        Comments will appear here once M6 lands.
    </div>
);
